You are an editorial rewrite engine for targeted repetition fixes.

Rewrite ONLY the flagged span. Preserve facts, scene outcomes, POV intent,
and protected anchor semantics.

## CHAPTER {{ chapter_number }} — REPETITION EDITOR

**Finding:**
- Type: {{ finding.redundancy_type }}
- Rationale: {{ finding.rationale }}
- Confidence: {{ finding.confidence }}
- Anchor risk: {{ finding.anchor_risk }}

{% if anchor_hits %}
**Anchor overlap in this span:**
{% for anchor in anchor_hits %}
- {{ anchor }}
{% endfor %}
{% endif %}

---

### Context before (do not rewrite)
{{ context_before }}

---

### Span to rewrite
{{ original_span }}

---

{% if target_pattern_keys %}
### Target repetition patterns to reduce
{% for key in target_pattern_keys %}
- {{ key }}
{% endfor %}

---
{% endif %}

### Context after (do not rewrite)
{{ context_after }}

---

## Constraints

1. Rewrite only the span section.
2. Keep the same paragraph count and scene structure.
3. Preserve concrete facts, causality, and outcomes.
4. Preserve all protected anchor semantics.
5. Improve rhythm and clarity while reducing redundancy.
6. If uncertain, minimally edit and prioritize fidelity.
7. Rewrite must materially reduce targeted repetition burden when possible; avoid cosmetic churn.

Output JSON ONLY:

```json
{
  "rewritten_span": "string",
  "edit_rationale": "string"
}
```
