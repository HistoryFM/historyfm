I want to now make this an automated backend that does the following

On any given day - the agent should generate 2 chapters for 2 novels 

We should have a backlog of dramatized historical novels - the characters which are POVs, detailed synopsis etc that can use be used to determine the structure

Once chapters are generated we should push them to frontend so they shop up in the webapp - it will have to be local first but we need to figure out the pipeline once I deploye the frontend somewhere- read @deploy_todo.md for more info here